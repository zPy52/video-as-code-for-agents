# my-videos

Consumer workspace for your own video projects. The SDK/source lives one
directory up in `video-as-code-for-agents`; this folder is where videos and
personal components live.

The main entry point is `src/index.ts`.

Render it with either command:

```bash
cd my-videos
pnpm render
```
